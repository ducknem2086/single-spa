```ts
project thử nghiệm tính năng github submodules
cho phép nhét nhiều repo vào 1 như các ứng dụng lớn
để mỗi ông muốn code là tạo ra 1 bản fork rồi push 
lên submodule thay vì kéo cả cụm về rồi dev .


## Cách mở mfe : 
host : npm i -> npm run serve (kéo từ module: host-single-spa @ 3d32110)
remote-2 : npm i -> npm run serve (kéo từ module remote 2)

